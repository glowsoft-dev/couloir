# Terminaison TLS devant le serveur Couloir.
#
# Caddy obtient et renouvelle le certificat tout seul. C'est délibérément lui
# qui porte le TLS et non Node : le jour où l'école a déjà un proxy, on retire
# ce service et on ne touche pas à l'application.
#
# COULOIR_DOMAINE est lu depuis le fichier .env voisin.

{$COULOIR_DOMAINE} {
	# Le certificat s'obtient par vérification DNS, jamais par HTTP.
	#
	# La vérification HTTP suppose le serveur joignable depuis Internet. Ici
	# il ne l'est pas, et c'est voulu : rien n'entre sur le réseau du campus.
	# La vérification DNS prouve qu'on contrôle la zone, pas que la machine
	# est atteignable — un enregistrement qui pointe vers une adresse privée
	# obtient donc un vrai certificat, reconnu par les écrans sans rien
	# installer sur eux.
	#
	# Configuré ici et non en option globale : `dns_challenge_override_domain`
	# n'existe pas au niveau global, et c'est elle qui porte tout l'intérêt du
	# montage.
	tls {
		dns ovh {
			endpoint {$OVH_ENDPOINT}
			application_key {$OVH_APPLICATION_KEY}
			application_secret {$OVH_APPLICATION_SECRET}
			consumer_key {$OVH_CONSUMER_KEY}
		}

		# La délégation du défi, et la raison d'être de tout ceci.
		#
		# L'établissement pose un CNAME une fois pour toutes, de
		# `_acme-challenge.<son-nom>` vers la valeur ci-dessous. Le
		# renouvellement écrit alors son enregistrement temporaire dans NOTRE
		# zone, et pas dans la sienne.
		#
		# Ce qu'on y gagne : aucun identifiant d'API sur la zone de
		# l'établissement n'a jamais à exister. Un jeton de zone, même
		# restreint, permet de réécrire les MX du courrier et le site public —
		# c'est beaucoup demander pour renouveler le certificat d'un écran de
		# couloir, et un service informatique a raison de le refuser.
		#
		# La valeur est le nom canonique COMPLET visé par le CNAME : Caddy ne
		# préfixe pas `_acme-challenge` tout seul.
		dns_challenge_override_domain {$COULOIR_DEFI_DELEGUE}
	}

	# Compression : la console fait 265 ko de JavaScript, servis à chaque
	# ouverture depuis un poste de l'école.
	encode zstd gzip

	# Les médias sont immuables — leur identifiant est leur empreinte. Un
	# écran qui redémarre ne doit pas retélécharger ce qu'il a déjà.
	@medias path /v1/assets/*
	header @medias Cache-Control "public, max-age=31536000, immutable"

	# Le manifeste, jamais. Un écran qui servirait un manifeste en cache
	# afficherait l'annonce de la semaine dernière.
	@api path /v1/*
	header @api Cache-Control "no-store"

	reverse_proxy serveur:3000 {
		# L'interrogation longue des commandes retient la réponse 25 s. Le
		# défaut de Caddy la couperait avant, et chaque écran repartirait
		# dans une reconnexion inutile toutes les quelques secondes.
		transport http {
			read_timeout 120s
		}
	}

	log {
		output file /var/log/caddy/acces.log {
			roll_size 20mb
			roll_keep 10
		}
	}
}
